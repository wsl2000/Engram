[project]
name = "engram-verification"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "datasets",
  "lm-eval",
  "numpy",
  "pytest",
  "pyyaml",
  "torch",
  "tqdm",
  "transformers",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

