VERDICT: ON-TRACK — correct, honest handling of an inconclusive primary; the unique-data pivot is the right next step. Reviewed at H10.7.

- Pair-1 primary = NOT VERIFIED / INCONCLUSIVE, correctly reported (honest-negative applied right). Knockout flat (TriviaQA off−on −0.0066, PopQA −0.019; EM floor); slices mixed. ✓
- The gate-α diagnostic settles the key question: **α saturated ~1.0 and Engram contribution GREW (final ΔRMS 0.041→0.080; layer-6 ratio 0.0039→0.0083) ⇒ the model IS using Engram ⇒ NOT a config/wiring bug.** So weak knockout = the repeated-4.69B washout (§6), exactly as diagnosed. The fixable-bug branch is cleanly ruled out. ✓
- One genuine positive: **repeated-n-gram slice favors Engram (A−B = −0.024 > global −0.003)** — slice-gap > global-gap in the right direction; depth median 1 layer earlier. Entity-proxy is negative (+0.016). Mixed = "active but data-starved," consistent.
- Pivot to the **h4 UNIQUE-20B pair (prior fix #3)** is correct; the **h5_eval doc-disjoint tranche** (whole-doc skip-tokens) correctly fixes the held-out contamination concern from review-0432Z. ✓ Bad torchrun launch caught/relaunched with srun ✓; storage recovered ✓; invariants/no-EP/bf16/AdamW intact ✓.

CALIBRATE EXPECTATIONS (important): 20B UNIQUE is ~4.3× more tail than pair-1 but still **~5× below the ~100B-token threshold** where Engram's long-tail job really exists (§6/§13). The h4 pair may SHARPEN the signal (watch the repeated-n-gram slice + any small knockout delta) but **may still be inconclusive**. If it is, the correct conclusion stands: *"Engram active but no distinct factual job below ~100B unique tokens; verification data-limited, not refuted."* Do NOT over-invest chasing it past 24h — report honestly. The true bottleneck remains DATA (HF 504s capped tokenization), not the model.

Timing: h4 pair ~2.1h/run ⇒ ~H15 + eval, within 24h. Reviewer next ~2h — reading the h4-pair knockout (on unique data) + the repeated-n-gram slice.
