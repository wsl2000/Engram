VERDICT: ISSUES — early knockout is WEAK (concerning, not yet conclusive). Reviewed at H8.0 (off-cycle, operator-requested). Wiring is live, so this is NOT the dead-path BLOCKER; the most likely cause is the repeated-data regime. Your plan (re-test at the matched 20B endpoint) is correct; do that + the checks below before any verdict.

## Read of the step-959 knockout
- answer-NLL delta (off−on): TriviaQA +0.0066, PopQA −0.013 ≈ no factual reliance yet. QA-EM 0.0/0.0 = floor (model can't answer this early) ⇒ EM-knockout uninformative here; answer-NLL is the right probe.
- Wiring diag shows Engram active but small (block2 RMS 0.001, block6 0.0037, final ΔRMS 0.035). So: wired, but not yet load-bearing. Step 959 is only ~3.8B tokens in — likely too early on its own.

## Most likely cause (matches handoff §6 / our prior washout) — diagnose, don't panic
pair-1 trains on **4.69B UNIQUE tokens sampled with replacement (~4.3× repeats)**. A 4.5B-param backbone simply MEMORIZES that small repeated set in its own weights ⇒ Engram has no distinct job ⇒ disabling it doesn't hurt ⇒ weak knockout. This is the **data-starved off-regime washout (§6)**, not necessarily a bug or a refutation of Engram. Engram's signal needs an UN-memorized rare-n-gram long tail, which ~4.7B unique tokens barely has.

## Do this before concluding
1. **Re-run knockout at the full pair-1 endpoint (A@5027 vs B@5027, ~20B)** — correct, in progress. Track the answer-NLL delta there, not EM (EM may still be near floor at 0.48B).
2. **Plot the gate α trajectory + Engram-contribution RMS over training.** If α/RMS are RISING → the model is learning to use Engram (good; weak knockout is then the data-redundancy). If α stays ~0 → the model isn't routing through memory: check the table LR (handoff §2 wants the Engram param-group at ×5 LR, wd 0) and the gate init — a suppressed gate is a fixable config issue, not a verdict.
3. **If endpoint knockout is still weak, the real fix is UNIQUE data, not config.** The 20B h4 tranche is unique (208 shards). Time permitting, run ONE fresh paired pair on the h4 tranche (both arms on it, paired) — that's the first setting where Engram could have a real job. Likely won't fit 24h; if so → honest-negative.
4. **Honest-negative framing (pre-agreed, §10 + sensitivity rule):** if knockout stays weak at the endpoint, do NOT conclude "Engram doesn't work." Conclude: *"at 0.48B / ~4.7B-unique-(repeated)-tokens the setup is below the data threshold where Engram has a distinct job (§6 washout); knockout/loss inconclusive."* Record the gate-α and contribution-RMS as the supporting evidence of "active but no job."

## Good
- Acked the held-out-data rule (slices/depth/loss on disjoint h4 tranche) ✓. Relaunched B to the matched endpoint ✓. Node-flakiness handling (now also avoiding cn02) with world size preserved ✓. Honest knockout reporting ✓.

— Reviewer (off-cycle). The endpoint knockout (answer-NLL delta) + the gate-α trajectory are the two things that decide this. Next auto-check ~2h.
