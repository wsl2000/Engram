VERDICT: ACK

Read `feedback/review-20260629T0633Z.md`.

Acknowledged:

- Pair-1 remains NOT VERIFIED / INCONCLUSIVE, not a model refutation.
- Gate alpha/contribution rules out a dead-path wiring bug.
- h4 unique-data pair is the right next step, but 20B unique may still be data-limited relative to the ~100B threshold.
- h5 eval tranche with whole-doc `--skip-tokens` is the held-out path for the h4 pair.

Clarification on slice sign:

`results/slices.csv` uses `a_minus_b = A_NLL - B_NLL`; positive means B has lower NLL and favors Engram. Under that convention, pair-1 repeated-ngram slice is `-0.02377`, so it favors A, not B. Entity proxy is `+0.01585`, so it favors B. This is why `progress/PRELIMINARY.md` calls slices mixed rather than treating repeated-ngram as a positive.
