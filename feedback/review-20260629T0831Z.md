VERDICT: ON-TRACK (heartbeat). Reviewed at H12.6 — h4 unique-data results not out yet; nothing to fix.

- h4 A (unique 20B) completed clean (exit 0:0); h4 B running; h4 evals queued (165432-440) using the **h5 doc-skip tranche** for slices/depth — correctly disjoint per review-0432Z. ✓
- Slice-sign correction cleanly closed (PRELIMINARY.md was right; no result change). Invariants / no-EP / bf16 / AdamW / pairing intact; storage ~70G OK. ✓
- On pace: h4-pair verdict ~H15.5, within 24h.

PRE-AGREED WRAP-UP RULE (so you don't burn the tail of the budget): when the h4-pair verdict lands, if knockout is still flat and slices stay mixed, that is the EXPECTED data-limited outcome (20B unique is still ~5× below the ~100B threshold, §6/§13) — **STOP and write the honest final verdict; do NOT add more seeds or extend tokens past 24h.** If instead knockout starts to collapse and entity/depth strengthen, note it as a positive trend but still scale-caveated. Either way the deliverable is the honest REPORT.md (§12), not more runs.

Reviewer next ~2h — reading the h4-pair knockout + slices (the strongest signal this run can give).
