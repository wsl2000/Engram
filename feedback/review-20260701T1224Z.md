VERDICT: ON-TRACK

Milestone + one actionable suggestion.

## Milestone: the #1 v1 killer is DEFEATED
- **≥200B data gate PASSED** (H7.3): 300B pool tokenized offline (uint32, doc-ID manifest), gate asserted. The streaming-504 death that capped v1 at 4.7B unique tokens is gone. This is the single biggest de-risk. ✓
- Tier-1 R-pilot set up correctly per §4: **A-only** difficulty pilot, `R_VALUES=1..32` sweep (freeze R from arm-A, no HARKing), F=5000 facts, `TARGET_TOKENS=20B` pre-gate, explicitly "does not wait for MFU/Tier-2 gates." Exactly right. ✓

## Suggestion (actionable): don't couple the CERTAIN verdict to the scarce 128-node window
- The Tier-1 **train** jobs carry `TRAIN_DEPENDENCY=afterok:168251` (the **128-H100** preflight), which is still `PENDING (AssocGrpGRES/Resources)`. That ties Tier-1 — the cheap, certain verdict — to the *hardest* resource to get (16 healthy nodes; v1's backfill slipped ~2 days).
- Tier-1's whole design point (§3) is that it's **decoupled and cheap**: 20B tokens/arm, small model, any MFU. It does **not** need 128 nodes — it runs fine on **1–2 nodes (8–16 H100)**, which are far easier/faster to allocate than the full 16-node block.
- **Suggest:** point Tier-1's train dependency at a **small-allocation preflight** (1–2 nodes), not the 128-node one, so the certain verdict lands in ~hours on whatever you can grab now, instead of waiting days for the full window. Keep the 128-node block reserved for **Tier-2** (which genuinely needs it). If your scheduler can't grant partial allocations, ignore this — but if it can, it protects the one result we're most confident about from the top timeline risk.

## Reminder for when Tier-1 trains (§4, silent killers)
- rung-0 must assert **train/eval key-n-gram token-ID identity** before trusting recall.
- Paired **answer-NLL** is the powered primary (EM floors); confirm single-token opaque subjects + large single-token object pool; keep the negative-control fact class (B−A gap < half main-class gap).

Great progress — data wall cleared, apparatus built, discipline intact. Reviewer watching ~every 2h.
