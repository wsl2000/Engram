VERDICT: PAUSED (operator released H100s at H14.7 — no run fault). State captured for the record.

## Final state at pause
- Operator canceled h4 B (job 165375) at step 4,668/5,027 (~18.4B tok) and the queued h5-disjoint evals. h4 B was final-only ⇒ **no checkpoint saved ⇒ no h4-pair verdict.** h4 A is complete (ckpt retained). VAST ~20T free.
- **Standing scientific result = pair-1 only: NOT VERIFIED / INCONCLUSIVE (data-limited).** Knockout flat (TriviaQA −0.0066 / PopQA −0.019), slices mixed (only entity-proxy +0.016 + depth median −1 layer faintly favor Engram), gate-α saturated ~1.0 with growing-but-non-load-bearing contribution ⇒ repeated-4.69B washout, NOT a bug and NOT a refutation of Engram.
- The cleaner test (h4, 20B UNIQUE) did not reach a verdict.

## If/when resumed
1. Relaunch h4 B seed 2024 from scratch (same h4 stream / world size / batch / optimizer / precision / 5,027-step endpoint), then the h5-disjoint knockout+slices+depth evals. **Lesson: enable periodic checkpoints (not final-only) so a cancellation doesn't discard the whole run.**
2. Expectation unchanged (review-0633Z/0831Z): 20B unique is ~5× below the ~100B-token threshold, so h4 may still be inconclusive — that would be the honest data-limited result, not a refutation.
3. The real unlock is DATA: to actually verify, push unique tokens toward ~100B (continue tokenization / use a pre-tokenized FineWeb-Edu mirror). Model/code/pipeline are sound; data quantity is the only blocker.

## Process note
All invariants held throughout (iso-param Δ=1,024, paired loader, no-EP, bf16, AdamW); honest-negative discipline applied correctly; feedback loop fully closed. Clean run — the limiter was tokenization throughput (HF 504s → 4.69B then 20B unique), not the science.

— Reviewer. Nothing is running now; the 2h auto-review will no-op until a run resumes.
