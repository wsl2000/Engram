VERDICT: ON-TRACK (for the 12h primary verdict). Excellent recovery from the H3 ISSUES — the pivot is correct and you're on pace for the primary knockout/slices verdict by ~H12. A few watch-items; one (held-out eval data) is important. Reviewed at H6.3.

## Correct calls — acknowledged
- Reduced-token pivot (20B faithful pair-1, primary-verdict-only, explicitly NOT a powered loss verdict): exactly right, matches §10 + prior fix #2. ✓
- Refusing to coarsen experts without operator sign-off: correct discipline — §2/§3 do fix 88↔68 as the only arm difference, and you flagged coarsening as an optional salvage rather than silently changing the science. Good. (See "Full run" below for when it is/ isn't worth it — you do NOT need it for the primary.) ✓
- Pairing preserved (B uses the same pair-1 stream as A); invariants re-asserted (Δ=1,024); AdamW / bf16 / no-EP / full-replication per spec; node exclusions with world size preserved; honest STATUS + fast ack + eval tooling staged + knockout preflight passed. ✓

## Make-or-break next milestone — surface it prominently
First B checkpoint ~H7.5 → the §10 BUG GATE: knockout (answer-NLL + QA-EM on/off, TriviaQA/PopQA). **If factual recall does NOT collapse, that is a BLOCKER (Engram path not wired) — stop and fix before trusting any number.** Report this result at the top of the next STATUS.

## Important watch-item — held-out eval data (contamination risk)
pair-1 trains on the 4.69B tranche **sampled with replacement** (~4.3× repeats), so:
- **Run targeted-slice + depth + loss eval on data DISJOINT from the training stream** (use the h4 20B tranche or a clean split, decontaminated by n-gram overlap). A "repeated-n-gram" slice computed on the repeated *training* stream is contaminated and meaningless — the slice-gap must be measured on HELD-OUT text.
- The with-replacement repetition further weakens the (already secondary, underpowered) global loss — keep claiming only the primary (knockout + slices) and state the honest-negative on loss. (Already doing this — good.)

## Full 6-run / loss corroboration not fitting 24h — acceptable
That criterion is secondary and underpowered by design (§10 power note). Deliverable = the faithful primary verdict + an honest schedule note. **Do NOT coarsen experts just to chase the loss signal** — that's a faithfulness deviation for an underpowered metric. Consider coarsening ONLY IF (a) the primary verdict is positive AND (b) the operator explicitly authorizes a powered loss follow-up — then apply it identically to both arms, re-assert iso-param, and document the deviation. For now: deliver the faithful pair-1 primary verdict; if time remains, prefer extending pair-1 tokens (faithful) over adding the underpowered seeds.

## Minor
- Entity slice = capitalized-token proxy: fine for v0, label it a proxy (real NER only if time).
- If more nodes drop, keep world size / batch / optimizer / data order identical (you are).

Strong work. Next auto-check ~2h — the H7.5 knockout result is what I'll be looking for.
