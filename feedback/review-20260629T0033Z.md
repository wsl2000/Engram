VERDICT: ISSUES — strong setup, but throughput is ~5–20× under target and the 24h/12h goals are at risk without a pivot. The science is NOT broken; this is an MFU/schedule problem. Reviewed at H3.0.

## Good — keep doing this
- Invariants asserted: active params 475,136,000 (both arms); non-embed Δ=1,024 ⇒ iso-param ✓; Engram sparse fraction 22.47% ✓.
- Paired-loader first-100-batch hash matched (synthetic + real shards) ✓ — the comparison is valid.
- AdamW fixed for all runs (Muon absent) — correct call, documented ✓.
- Node-health handling (exclude cn17/cn34 OOM-on-empty-GPU), clean 80-GPU nodelist ✓. STATUS is honest and detailed ✓.

## The problem that matters
Pure-PyTorch **Python-loop MoE**, no grouped-GEMM / Megablocks / Tutel / FlashAttn / TransformerEngine in the image. Measured **MFU 6.35%**, 1.41M tok/s @64GPU (grouped-mm); extrapolates ~1.76M tok/s @80GPU = **~4.7× under the 8.34M tok/s plan target** (loop backend is ~20×under). ⇒ 70B/run ≈ 11h, **six runs do NOT fit 24h**, and at H3 you're still in calibration. Risk: the whole budget gets spent on infra and no knockout/slice verdict is ever produced.

## Fixes — priority order
1. **Coarsen the experts — the biggest MFU lever on a no-kernel stack.** Fine-grained 88/68 routed experts is the root cause (per-expert Python dispatch). Switch to FEW big routed experts + a heavy shared expert (e.g. ~8–16 routed), then **re-run config_gen to RE-ASSERT iso-param/iso-FLOP between the two arms**. handoff §3 already states the binding constraint is the iso-param match, *not* the exact expert count — so this is an ALLOWED deviation for a verify-the-effect run (not a faithful-granularity repro); just document it. (Direct precedent: the upstream team hit this exact Python-loop-MoE wall and went 7%→16% MFU precisely by coarsening to heavy-shared + few routed.)
2. **Prioritize ONE paired pair → the primary verdict; cut tokens.** §10's primary verdict (knockout + targeted slices) needs ONLY pair 1 and is robust to far fewer tokens. Get arm-A + arm-B (seed 1337) to a checkpoint where knockout collapses factual recall, at whatever token budget fits (~20–40B is plenty for knockout/slices). That delivers the ≤12h preliminary even if the full 6-run/70B plan can't fit. **DEFER the 2 extra seeds** — they only feed the underpowered secondary loss (§10 power note).
3. **Time-box the infra optimization.** Lock in "good enough" throughput (fix #1 + grouped-mm at mbs2) and START pair-1 training now; stop chasing kernels. The deliverable is the science (knockout/slices), not peak MFU.
4. **Tokenization is also on the critical path.** Only ~4.69B tokens flushed so far (HF 504s). Even a reduced run needs ≥ its token budget available — add retry/backoff, shrink per-request size, or use a pre-tokenized FineWeb-Edu mirror, and keep the streamer ahead of training.

## Do NOT break while fixing
Hold every invariant: iso-param/iso-FLOP (re-assert after coarsening), paired identical data per seed, **no expert-parallel**, **bf16 not FP8**, identical optimizer/world-size/batch across all runs. Apply expert-coarsening IDENTICALLY to both arms. The §10 bug gate still stands: the first thing to confirm once pair-1 trains is that **knockout collapses TriviaQA/PopQA** — if it does not, STOP and fix wiring before trusting any number.

— Reviewer (auto, ~every 2h). Ack this in progress/STATUS.md; next check in ~2h.
