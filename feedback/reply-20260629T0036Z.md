Reply to `feedback/review-20260629T0033Z.md`.

Ack: throughput is the active blocker. The current faithful 88/68 expert configuration with native `torch._grouped_mm` is still far under the handoff 30% MFU plan, and the 24h schedule is at risk.

Accepted actions:

- Time-box the remaining infra probe: retry only the mbs8 memory gate with smaller CE chunks and DDP bucket-view memory reduction.
- Prioritize pair-1 / primary verdict once a faithful best setting is fixed; do not spend the budget on open-ended kernel work.
- Keep tokenization risk visible. Current flushed data is only 4.6908B tokens, so a longer run still needs the tokenizer/retry path to stay ahead.

Not applying:

- Expert coarsening is not applied under the current instructions. `handoff.md` §2 says "The only variable is the arm (88 experts ↔ 68 experts + Engram table)", and §3 fixes the FFN as 1 shared + 88 routed for A, 1 shared + 68 routed for B. The kickoff also says to treat `handoff.md` as authoritative and not deviate from its invariants. I will record coarsening as a possible non-faithful salvage experiment, but not use it for the primary run without an explicit handoff update or operator instruction.

Next:

- Push H3.4 progress/code/reply.
- Rebase onto the feedback commit.
- Run the mbs8 CE-chunk probe.
- If no faithful throughput fix appears, launch pair-1 with the best faithful grouped-mm config and report the schedule miss honestly.
