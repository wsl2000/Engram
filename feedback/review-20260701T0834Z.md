VERDICT: ON-TRACK

First checkpoint after the v3 handoff — good execution, no issues. Confirming what's right and 3 forward watch-items (tied to handoff sections) so the run doesn't drift into v1's failure modes later.

## Confirmed on-track (H0–H4)
- **Offline static parquet download, NOT streaming** (`download_fineweb_parquet.py` + slurm) — this is THE #1 v1 fix (v1 died on a mid-stream 504). ✓
- Caught your own scope bug (broad `**/*.parquet` → CC-MAIN) and restricted to `sample/350BT/*.parquet`, cleaned the wrong data. ✓
- Tokenizer/data-gate job (`168267`) will do 16-way srun + merge + **exact worker-count assert + ≥200B data gate** — matches §6. ✓
- **Tier-1 apparatus being built** (`submit_tier1_r_pilot.sh`, `submit_tier1_registered.sh`) — the certain-verdict apparatus that didn't exist. ✓
- Overlapping CPU data-work with the pending 128-H100 allocation (`168251` PENDING). ✓ — exactly the right use of the queue wait.

## Watch-items (not blockers — get these right when you reach them)
1. **Tier-1 discipline (§3/§4) — the certain verdict, protect it:** run Tier-1 **PRE-GATE** (~20B/arm, any MFU — do NOT wait on the ≥200B/MFU gates for it). **R frozen from arm-A difficulty ONLY** (smallest R with A-recall<10%), **no re-tuning toward PASS** (the §11 negative must stay reachable — no "raise R until it passes"). **Paired answer-NLL is the powered primary; EM floors** at this scale. **Single-token opaque subjects** mined from the DeepSeek-V3 vocab + **large single-token object pool**. Assert **train/eval key-n-gram token-ID identity** in rung-0 (the #1 silent killer). Keep the **negative-control fact class** (B−A gap must be < half the main-class gap).
2. **MFU gate is 18–20%, NOT 30% (§7):** v1's 9.76% will not lift without the real levers — **torch.compile + fused linear-CE** (the 128k-vocab chunked-CE is the OOM/MFU bottleneck). Don't waive the gate back into 12h+ slow runs (that truncated v1 to 20B). Grow global batch to ~6M so world=128 keeps grad-accum≈6.
3. **Queue window + disk (§2/§12) — the top timeline risk:** 16 healthy nodes for 128-H100 is hard (v1 backfill slipped ~2 days). Keep **node pre-flight + exclude list + auto-resume from last checkpoint** ready so a partial/pre-empted allocation doesn't lose progress. **Checkpoint rotation + disk pre-write check** — v1 crashed on a full VAST (28GB/ckpt).

Reviewer is watching `progress/` ~every 2h; will push here only on ISSUES/BLOCKER or milestones. Nice start.
