VERDICT: ON-TRACK

Adoption confirmed + fact-set discipline is correct + 2 timely efficiency suggestions (jobs still PENDING, so free to apply now — they speed up the *certain verdict*).

## Adopted + correct
- **Decouple accepted** (H8.9): Tier-1 pivoted to a **2-node/16-H100** path, 128-dep jobs canceled, 2-node preflight PASSED. The certain verdict is no longer hostage to the 16-node window. Exactly right. ✓
- **Fact set is textbook §4:** 5000 facts, **all single-token** subjects AND objects (`single_subjects=5000`, `overlap=0`), plus the **negative-control class (500)** vs main (4500). This is the discipline that makes rung-1 non-trivial and non-circular. ✓
- Mixed-stream injection smoke runs (doc manifest + shards). Good — just make sure rung-0 also asserts **train/eval key-n-gram token-ID identity** before trusting recall numbers.

## Two efficiency suggestions (optional, but timely — pilots are PENDING)
1. **The R-pilot doesn't need the full 20B per R.** Its only job is to measure **A-recall vs R** to freeze R (smallest R with A-recall<10%) — per §4 a **~5B/R** A-only pilot suffices. You're queuing 20B × 6 R-values = ~120B of A-only compute just to pick R. Suggest **~5B/R for the pilot**, then spend the full **20B on the registered A+B run at the frozen R**. Saves ~75% of pilot compute → certain verdict lands much sooner.
2. **Turn on the memory-efficient CE (+`torch.compile`) for the Tier-1 trains too.** The R1/R2 runs are at ~300k tok/s on 16 H100 ≈ **~7% MFU** — that's the v1 regime. You already built memory-efficient linear CE (H1.7); applying it + compile to Tier-1 (not just reserving it for Tier-2) should give ~2–3× and get the certain verdict out in ~a day instead of several. (Tier-1 tolerates any MFU, so this is purely speed, not a gate.)

Net: you've cleared the data wall, decoupled the verdict, and built the apparatus with the right controls. These two tweaks just get the answer faster. Reviewer watching ~2h.
